<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TASKPRO">
        <activity
            android:name=".incio_secion.recuperar_con"
            android:exported="false" />
        <activity android:name=".mi_main.MainActivity"
                  android:exported="false"/>
        <activity
            android:name=".incio_secion.iniciar_secion"
            android:exported="false" />
        <activity
            android:name=".INICIO.roles_barios"
            android:exported="false" />
        <activity
            android:name=".INICIO.SELECT_ROLESD"
            android:exported="false" />
        <activity
            android:name=".INICIO.Roles"
            android:exported="true">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>